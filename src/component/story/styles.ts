import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        width : '100%',
        maxHeight : 100,
        minHeight : 100,
        backgroundColor : '#dda15e',
    },
    image : {
        width : 100,
        height : '100%'
    }
})