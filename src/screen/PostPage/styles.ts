import { StyleSheet } from "react-native";

export const mainStyles = StyleSheet.create({
    route : {
        position : 'absolute',
        zIndex : 2,
        right : 12,
        top : 12
    }
})