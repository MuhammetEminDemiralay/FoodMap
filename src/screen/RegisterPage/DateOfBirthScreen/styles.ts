import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

    calenderBtn: {
        width: '80%',
        height: 45,
        borderWidth: 1,
        alignItems: 'center',
        justifyContent: 'center',
        borderColor: '#000814',
        borderRadius: 10,
        backgroundColor: '#fff'
    },
    calenderText: {
        fontSize: 20,
        fontWeight: '500'
    }

})